/** Shared fleet localStorage keys (mirrors Tactical-Infinity grudgeFleetBridge). */
export const FLEET_STORAGE_KEYS = {
  deviceId: "grudge_device_id",
  activeCharacter: "grudge_active_character",
  legacyActiveCharacter: "gruda_active_character",
} as const;
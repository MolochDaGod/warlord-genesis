[build]
builder = "NIXPACKS"
buildCommand = "cd api && npm install"

[deploy]
startCommand = "cd api && npm run db:migrate && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5